# XQuality Machine 32 ablation notebook

Open `layer_ablation_machine32.ipynb` from the repository root or from this folder.

The notebook supports:

- full NeoOLAF runs,
- partial layer runs,
- resume from a previous `state.json`,
- prompt-size inspection per layer,
- layer-wise evaluation against XQuality Machine 32 ground truth in JSON or Excel.
