# EventStoryLine one-document native NeoOLAF v1.5

Apply the patch at the NeoOLAF repository root, then run the v1.5 notebook.
The experiment still uses the RAGTree OWL-Time ontology and the controlled
`PRECONDITION` / `FALLING_ACTION` task schema. Gold entities and relations are
loaded only after Layer 12 for evaluation.

v1.5 is still a one-document development run. Do not freeze the five-document
configuration until the atomic event and verified relation diagnostics have been
reviewed.
